# YueFa 项目开发流程总览

本文档提供 YueFa 项目完整的开发流程指南，确保版本迭代快速、稳定、有序。

## 🎯 快速开始

YueFa 项目配备了 8 个 Claude Skills，覆盖从功能开发到部署的完整流程:

| Skill | 用途 | 何时使用 |
|-------|------|----------|
| [/feature](../../.claude/skills/feature.md) | 新功能开发规划 | 开始开发新功能时 |
| [/code](../../.claude/skills/code.md) | 代码编写规范检查 | 编写代码时 |
| [/test](../../.claude/skills/test.md) | 测试编写与运行 | 完成代码后 |
| [/commit](../../.claude/skills/commit.md) | 智能 Git 提交 | 提交代码时 |
| [/ci-check](../../.claude/skills/ci-check.md) | CI/CD 预检查 | Push 前验证 |
| [/fix](../../.claude/skills/fix.md) | 问题修复流程 | 发现 Bug 时 |
| [/review](../../.claude/skills/review.md) | 代码审查 | 审查 PR 时 |
| [/refactor](../../.claude/skills/refactor.md) | 代码重构 | 改进代码时 |

## 📋 标准开发流程

### 流程 1: 新功能开发

```
1. 功能规划
   ↓ 使用 /feature
   输出: 功能设计文档

2. 代码实现
   ↓ 使用 /code
   符合编码规范

3. 编写测试
   ↓ 使用 /test
   单元测试 + E2E 测试

4. 提交代码
   ↓ 使用 /commit
   规范化提交信息

5. CI 预检查
   ↓ 使用 /ci-check
   本地模拟 CI

6. Push 代码
   ↓ git push
   触发 CI/CD

7. 自动部署
   ↓ GitHub Actions
   部署到生产环境
```

### 流程 2: Bug 修复

```
1. 问题排查
   ↓ 使用 /fix
   复现 → 分析 → 定位

2. 编写测试
   ↓ 使用 /test
   先写测试,再修复

3. 修复代码
   ↓ 使用 /code
   修复并验证

4. 提交和部署
   ↓ 使用 /commit → /ci-check
   提交并验证
```

### 流程 3: 代码重构

```
1. 重构规划
   ↓ 使用 /refactor
   识别目标 → 评估风险

2. 补充测试
   ↓ 使用 /test
   确保测试覆盖

3. 小步重构
   ↓ 使用 /code
   频繁测试 → 频繁提交

4. 验证部署
   ↓ 使用 /ci-check
   确保行为不变
```

## 🏗️ 项目架构

### 技术栈总览

**前端**:
- Vue 3 + Vite + Vant
- Pinia + Vue Router
- Axios + TailwindCSS

**后端**:
- Express + PostgreSQL/SQLite
- JWT + bcryptjs
- express-validator

**测试**:
- Vitest (单元测试)
- Playwright (E2E测试)
- Supertest (API测试)

**CI/CD**:
- GitHub Actions
- 阿里云 OSS (前端)
- 阿里云函数计算 (后端)

### 目录结构

```
yueFa/
├── .claude/
│   └── skills/              # Claude Skills
│       ├── feature.md
│       ├── code.md
│       ├── test.md
│       ├── commit.md
│       ├── ci-check.md
│       ├── fix.md
│       ├── review.md
│       └── refactor.md
│
├── YueFa-docs/              # 项目文档
│   ├── standards/           # 编码规范
│   │   ├── frontend.md
│   │   ├── backend.md
│   │   ├── testing.md
│   │   └── commit.md
│   └── workflows/
│       └── development.md   # 本文档
│
├── codes/                   # 源代码
│   ├── packages/
│   │   ├── web/            # 前端应用
│   │   └── server/         # 后端应用
│   └── pnpm-workspace.yaml
│
└── .github/
    └── workflows/
        └── ci-cd.yml        # CI/CD 配置
```

## 🔄 CI/CD 流程

### GitHub Actions 自动化

```
Push to main
    ↓
1. Unit Tests (单元测试)
   ├─ 前端测试 (Vitest)
   └─ 后端测试 (Vitest)
    ↓
2. E2E Tests (E2E 测试)
   └─ Playwright
    ↓
3. Build (构建)
   └─ Vite build
    ↓
4. Deploy Frontend (部署前端)
   └─ 阿里云 OSS
    ↓
5. Deploy Backend (部署后端)
   └─ 阿里云函数计算
    ↓
✅ 部署完成
```

### 本地 CI 预检查

```bash
# 完整检查
pnpm lint && pnpm test && pnpm test:e2e && pnpm build:web

# 或使用 Skill
# 使用 /ci-check
```

## 📦 常用命令

### 开发命令

```bash
# 启动开发环境
pnpm dev              # 前后端并行启动
pnpm dev:web          # 仅前端
pnpm dev:server       # 仅后端

# Docker 开发环境
pnpm docker:dev       # 启动 PostgreSQL + 服务
pnpm docker:down      # 停止服务
```

### 测试命令

```bash
# 单元测试
pnpm test             # 所有测试
pnpm test:web         # 前端测试
pnpm test:server      # 后端测试

# E2E 测试
pnpm test:e2e         # 运行 E2E
pnpm --filter @yuefa/web test:e2e:ui  # UI 模式

# 覆盖率
pnpm test:coverage
```

### 构建和部署

```bash
# 构建
pnpm build            # 构建所有
pnpm build:web        # 构建前端

# 代码检查
pnpm lint             # ESLint 检查
pnpm lint --fix       # 自动修复

# 部署
git push origin main  # 自动触发 CI/CD
```

## 🎓 学习路径

### 新成员快速上手

1. **环境搭建** (30分钟)
   ```bash
   # 克隆项目
   git clone <repo-url>
   cd yueFa/codes

   # 安装依赖
   pnpm install

   # 启动开发环境
   pnpm dev
   ```

2. **熟悉架构** (1小时)
   - 阅读项目文档
   - 了解技术栈
   - 理解目录结构

3. **开发第一个功能** (2小时)
   - 使用 `/feature` 规划
   - 使用 `/code` 编写
   - 使用 `/test` 测试
   - 使用 `/commit` 提交

4. **熟练使用** (持续)
   - 掌握所有 Skills
   - 理解 CI/CD 流程
   - 独立开发功能

## 📚 核心文档索引

### 开发规范
- [前端编码规范](../standards/frontend.md)
- [后端编码规范](../standards/backend.md)
- [测试编写规范](../standards/testing.md)
- [Git 提交规范](../standards/commit.md)

### Claude Skills
- [/feature - 功能开发](../../.claude/skills/feature.md)
- [/code - 代码规范](../../.claude/skills/code.md)
- [/test - 测试编写](../../.claude/skills/test.md)
- [/commit - 提交规范](../../.claude/skills/commit.md)
- [/ci-check - CI 预检查](../../.claude/skills/ci-check.md)
- [/fix - 问题修复](../../.claude/skills/fix.md)
- [/review - 代码审查](../../.claude/skills/review.md)
- [/refactor - 代码重构](../../.claude/skills/refactor.md)

### CI/CD
- [GitHub Actions 配置](../../../.github/workflows/ci-cd.yml)

## 🚀 最佳实践

### 开发最佳实践
1. ✅ 使用 Skills 指导开发
2. ✅ 小步提交,频繁集成
3. ✅ 测试先行,确保质量
4. ✅ 代码审查,互相学习
5. ✅ 持续重构,保持整洁

### 质量保证
1. ✅ 单元测试覆盖核心逻辑
2. ✅ E2E 测试覆盖关键流程
3. ✅ 代码符合编码规范
4. ✅ 提交信息清晰规范
5. ✅ CI 通过后再部署

### 团队协作
1. ✅ 功能开发前先设计
2. ✅ 重大变更先讨论
3. ✅ 代码审查认真负责
4. ✅ 问题及时沟通
5. ✅ 文档及时更新

## 💡 常见问题

### Q1: 如何快速开始开发?
A: 使用 `/feature` Skill 进行功能规划，然后按照标准流程开发。

### Q2: 测试如何编写?
A: 使用 `/test` Skill，参考现有测试用例，确保覆盖正常、异常、边界场景。

### Q3: CI 失败怎么办?
A: 使用 `/ci-check` 在本地预检查，根据错误信息修复问题。

### Q4: 如何保证代码质量?
A: 遵循编码规范 (`/code`)，编写充分测试 (`/test`)，做好代码审查 (`/review`)。

### Q5: 遇到 Bug 如何处理?
A: 使用 `/fix` Skill，系统化地复现→分析→修复→测试→部署。

## 🔗 外部资源

- [Vue 3 文档](https://cn.vuejs.org/)
- [Express 文档](https://expressjs.com/)
- [Vite 文档](https://vitejs.dev/)
- [Vitest 文档](https://vitest.dev/)
- [Playwright 文档](https://playwright.dev/)
- [约定式提交规范](https://www.conventionalcommits.org/zh-hans/)

---

**更新时间**: 2026-01-21
**维护者**: YueFa 开发团队
